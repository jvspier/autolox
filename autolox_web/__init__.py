"""autolox_web — phase-3 web app for the bulk NFC enrolment workflow.

Iteration 1 goals in docs/phase-3-spec.md. Runs as a FastAPI service; the
same code is used locally with `uvicorn autolox_web.app:app --reload` and
in the LXC container.
"""
